import styled, { css } from 'styled-components';

export const Container = styled.div`
  position: relative;
  padding-bottom: 1.6rem;
  margin-bottom: 1rem;
  &:last-child {
    margin-bottom: 0px;
  }
`;
