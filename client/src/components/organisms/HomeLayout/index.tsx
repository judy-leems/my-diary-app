import React from 'react';
import { Outlet } from 'react-router-dom';
import * as UI from './style';

const HomeLayout = () => {
  return <UI.AppContainer>HomeLayout</UI.AppContainer>;
};

export default HomeLayout;
