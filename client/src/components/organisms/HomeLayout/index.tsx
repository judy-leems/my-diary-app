import React from 'react';
import { Outlet } from 'react-router-dom';
import * as UI from './style';

const HomeLayout = () => {
  return <UI.Container>HomeLayout</UI.Container>;
};

export default HomeLayout;
