import React from 'react';
import { HomeContainer, HomeStart, HomeMessage } from '@components/index';

const Home = () => {
  return (
    <HomeContainer>
      <HomeMessage />
      <HomeStart />
    </HomeContainer>
  );
};

export default Home;
